do:
	sudo apt-get install gaps
	sudo apt-get install feh
	sudo apt-get install rofi
	sudo apt-get install compton
	cp *.* -r -u -f ./i3 ~/.config/i3
	cp *.* -r -u -f ./thunar ~/.config/thunar
	cp ./android_notification.rasi -r -u -f	/usr/share/rofi/themes/.

